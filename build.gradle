buildscript {
    repositories { jcenter() }
    dependencies { classpath 'com.netflix.nebula:nebula-plugin-plugin:2.4.+' }
}

description 'Aggregates Javadocs across all projects in a multi-project build'

apply plugin: 'nebula-plugin'
apply plugin: 'idea'

contacts {
    'benjamin.muschko@gmail.com' {
        moniker 'Benjamin Muschko'
        github 'bmuschko'
    }
}

dependencies {
    testCompile('com.netflix.nebula:nebula-test:2.4.+') {
        exclude group: 'org.codehaus.groovy'
    }
}